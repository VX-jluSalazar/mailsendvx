{% form_theme configurationForm '@PrestaShop/Admin/TwigTemplateForm/prestashop_ui_kit.html.twig' %}
{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
  <section class="mailsendvx-shell">
    <div class="mailsendvx-hero">
      <div class="mailsendvx-hero__copy">
        <span class="mailsendvx-eyebrow">{{ 'Vista de monitoreo'|trans({}, 'Modules.Mailsendvx.Admin') }}</span>
        <h1 class="mailsendvx-page-title">{{ 'Dashboard de Mail Send VX'|trans({}, 'Modules.Mailsendvx.Admin') }}</h1>
        <p class="mailsendvx-page-subtitle">
          {{ 'Revisa el estado de las plantillas y la carga de la cola antes de analizar los logs individuales de envío.'|trans({}, 'Modules.Mailsendvx.Admin') }}
        </p>
      </div>
      <div class="mailsendvx-status-cluster">
        <div class="mailsendvx-metric">
          <span class="mailsendvx-metric__label">{{ 'Templates'|trans({}, 'Modules.Mailsendvx.Admin') }}</span>
          <strong class="mailsendvx-metric__value">{{ templates_count }}</strong>
        </div>
        <div class="mailsendvx-metric">
          <span class="mailsendvx-metric__label">{{ 'Programados'|trans({}, 'Modules.Mailsendvx.Admin') }}</span>
          <strong class="mailsendvx-metric__value">{{ scheduled_count }}</strong>
        </div>
        <div class="mailsendvx-metric">
          <span class="mailsendvx-metric__label">{{ 'Pendientes'|trans({}, 'Modules.Mailsendvx.Admin') }}</span>
          <strong class="mailsendvx-metric__value">{{ pending_count }}</strong>
        </div>
      </div>
    </div>

    <nav class="mailsendvx-tabs" data-dashboard-tabs aria-label="{{ 'Secciones del dashboard'|trans({}, 'Modules.Mailsendvx.Admin') }}">
      <a class="mailsendvx-tab {{ activeTab == 'events' ? 'is-active' : '' }}" data-dashboard-tab="events" href="{{ path('mailsendvx_dashboard', {tab: 'events'}) }}">
        {{ 'Eventos capturados'|trans({}, 'Modules.Mailsendvx.Admin') }}
      </a>
      <a class="mailsendvx-tab {{ activeTab == 'queue' ? 'is-active' : '' }}" data-dashboard-tab="queue" href="{{ path('mailsendvx_dashboard', {tab: 'queue'}) }}">
        {{ 'Estado de la cola'|trans({}, 'Modules.Mailsendvx.Admin') }}
      </a>
      <a class="mailsendvx-tab {{ activeTab == 'logs' ? 'is-active' : '' }}" data-dashboard-tab="logs" href="{{ path('mailsendvx_dashboard', {tab: 'logs'}) }}">
        {{ 'Logs operativos'|trans({}, 'Modules.Mailsendvx.Admin') }}
      </a>
      <a class="mailsendvx-tab {{ activeTab == 'configuration' ? 'is-active' : '' }}" data-dashboard-tab="configuration" href="{{ path('mailsendvx_dashboard', {tab: 'configuration'}) }}">
        {{ 'Configuracion'|trans({}, 'Modules.Mailsendvx.Admin') }}
      </a>
    </nav>

    <div class="mailsendvx-tab-panels" data-dashboard-panels data-active-tab="{{ activeTab }}">
      {{ include('@Modules/mailsendvx/views/templates/admin/dashboard_tab_panels.html.twig') }}
    </div>
  </section>
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script>
    {{ include('@Modules/mailsendvx/views/js/mailsendvx-grid.bundle.js')|raw }}
  </script>
  <script src="{{ asset('themes/default/js/bundle/pagination.js') }}"></script>
{% endblock %}
